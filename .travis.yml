sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -f Dockerfile.dev -t niallquigley/docker-react .

script:
  - docker run -e CI=true niallquigley/docker-react npm run test -- --coverage
  # - docker build -t niallquigley/docker-react .
  # - docker run niallquigley/docker-react
